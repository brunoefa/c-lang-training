#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
FUAQ Leia 5 nomes e escreva na tela todos os nomes que possuam mais de 15 caracteres.
*/

int main() {

    char nome[25];

    printf("%c", 65);

    /*
    for(int i = 0; i < 5; i++) {
        printf("Informe o nome %d: ", i + 1);
        gets(nome);
        if (strlen(nome) > 15){
            printf("Nome selecionado: %s\n", nome);
        }
    }
    */
    return 0;
}
